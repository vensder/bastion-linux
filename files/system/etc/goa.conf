# Bastion Linux: no online account providers.
# "none" matches no provider, so GNOME Online Accounts loads none.
# (An empty "enable=" would mean "all".) Root-owned: users cannot override it.
[providers]
enable=none
